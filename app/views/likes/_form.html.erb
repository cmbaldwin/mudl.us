<%= form_with(model: like, local: true) do |form| %>
  <% if like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
        <% like.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tweetId %>
    <%= form.text_field :tweetId %>
  </div>

  <div class="field">
    <%= form.label :fullText %>
    <%= form.text_field :fullText %>
  </div>

  <div class="field">
    <%= form.label :expandedUrl %>
    <%= form.text_field :expandedUrl %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
